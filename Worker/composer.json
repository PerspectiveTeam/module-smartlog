{
    "name": "perspective/smartlog-deps",
    "description": "Isolated dependencies for Perspective_SmartLog module (avoids PSR conflicts with Magento 2)",
    "require": {
        "php": ">=8.1",
        "opensearch-project/opensearch-php": "^2.0",
        "theodo-group/llphant": "^0.11"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
